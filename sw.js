// Service Worker Básico para funcionalidad PWA
self.addEventListener('fetch', function(event) {
  // Por ahora, solo nos aseguramos de que el service worker exista
  // para que el navegador considere el sitio como instalable.
});
